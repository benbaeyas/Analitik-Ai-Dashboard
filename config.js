// config.js — Public configuration (NO secrets here)
const CONFIG = {
  // API key is stored server-side in .env — never exposed to the browser.
  AI_ENDPOINT: '/api/ai',
  Groq_MODEL: 'llama-3.3-70b-versatile'
};
